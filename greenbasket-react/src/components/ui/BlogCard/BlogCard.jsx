// BlogCard Component
// TODO: Build this reusable UI component
import styles from './BlogCard.module.css';

function BlogCard(props) {
  return (
    <div className={styles.wrapper}>
      <p>BlogCard – build me!</p>
    </div>
  );
}

export default BlogCard;
